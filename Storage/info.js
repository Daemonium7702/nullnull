/*const mongoose= require("mongoose");
const mySexySchema = mongoose.Schema({
_id: mongoose.Scheme.Types.ObjectId(),
username: String,
bank: String,
cash: String,
userid: String,
})
module.exports = mongoose.model("Sexy", mySexySchema)
if(command === "bal"){
const mongoose = require("mongoose")
const money = require("../Storage/info.js")
mongoose.connect("mongodb//:localhost/Sexy")
	const Money = new money({
	_id: mongoose.Schema
	})
}*/
